import 'package:flutter/material.dart';

class ProfileVideos extends StatelessWidget {
  const ProfileVideos({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Profile Videos');
  }
}
