import 'package:flutter/material.dart';

class ProfilePhotos extends StatelessWidget {
  const ProfilePhotos({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Profile Photos');
  }
}
