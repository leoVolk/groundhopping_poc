import 'package:flutter/material.dart';

class ProfileComments extends StatelessWidget {
  const ProfileComments({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Profile Comments');
  }
}
