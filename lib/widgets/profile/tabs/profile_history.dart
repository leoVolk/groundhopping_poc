import 'package:flutter/material.dart';

class ProfileHistory extends StatelessWidget {
  const ProfileHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Profile History');
  }
}
